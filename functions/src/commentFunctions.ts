/* eslint-disable */
import { firestore, functions, firestoreCommentRef } from "./firebase";
import { ResponseCodes } from "./responseCode";

/**
 * 5-02. 특정 댓글 수정
 */
export const updateComment = functions.https.onCall(async (data, context) => {
    try {
    } catch (error) {
    }
});

/**
 * 5-03. 특정 댓글 삭제
 */
export const deleteComment = functions.https.onCall(async (data, context) => {
    try {
    } catch (error) {
    }
});